# feature-0008: Tests

## Maze

1. `mazeFileExists`: el fichero de la clase del laberinto existe.
2. `mazeClassAndGenerateMapDeclared`: la clase Maze declara la generación del mapa.
3. `mazeDeclaresDefaultDimensions`: la clase Maze declara las dimensiones por defecto (32×32, profundidad 5).
4. `mazeDeclaresRoomEncodings`: la clase Maze declara las codificaciones de las habitaciones.
5. `mazeDeclaresLevelsAndVisibility`: la clase Maze declara los niveles y la visibilidad.
6. `mazeImplementsDrunkenMinerGeneration`: la clase Maze implementa la generación tipo "drunken miner".
7. `mazeDeclaresFieldOfViewMasks`: la clase Maze declara los campos de visión (norte, sur, este, oeste).
8. `mazeInitializesFieldOfView`: la clase Maze inicializa la visibilidad inicial.
9. `mazeResourcesAreServed`: la clase Maze es servida por el servidor.
